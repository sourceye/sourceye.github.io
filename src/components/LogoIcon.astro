---
export interface Props {
  isFollowable?: boolean;
}

const { isFollowable = false } = Astro.props;
---

<svg viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg" class="stroke-black dark:stroke-white">
  <path d="M8 12.5 L 1 16 L 8 19.5 M 24 12.5 L 31 16 L 24 19.5" fill="none" stroke-width="2"></path>
  <circle id={`${isFollowable ? 'sourceye' : ''}`} cx="50%" cy="50%" r="5" fill="none" stroke-width="3"></circle>
</svg>
